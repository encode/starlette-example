* Cleanup Starlette call stack.
* Specific error when `None` is returned from an endpoint.
* Proper info in debug when `NoMatchFound` is raised by `url_for()`
* Ensure 'request' is not required by `TemplateResponse`
